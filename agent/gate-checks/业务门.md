# 业务门自动验收

> 基于 `../../templates/门禁卡-业务门.md` 转化的可执行评估项。
> 每项：检查什么 → 怎么检查 → 通过标准 → 证据要求。
> 智能体调用本文件，逐项收集证据并判定通过/不通过。

---

## 过门禁标准（8 项硬指标）

### 指标 1：所有 P0 功能已开发完成

- **检查什么**：功能清单里的 P0 功能是否全部交付
- **怎么检查**：对照 `docs/a-立项文档.md` 的功能清单，逐项核对完成状态
- **通过标准**：P0 全部完成；P1/P2 有明确的"未做/延后"说明（不能是"基本完成"）
- **证据要求**：功能清单核对表 + 每项的完成状态

### 指标 2：每轮开发都有验收证据

- **检查什么**：业务模块是否按"一轮一验收"推进，而非一口气写完
- **怎么检查**：检查每轮的验收记录（文件 / 命令输出 / 截图）
- **通过标准**：每轮都有功能走通证据（执行命令 + 输出）；没有"本轮范围锁死"之外的任务混入
- **证据要求**：每轮验收记录路径 + 关键输出

### 指标 3：每个模块走过测试六步法

- **检查什么**：模块完成前是否走 `testing-strategy` 六步法
- **怎么检查**：核对模块的测试记录（六步法各步的证据）
- **通过标准**：每个模块的测试记录完整（含"假测试/过度测试"自查）
- **证据要求**：测试记录 + 运行输出

### 指标 4：安全审查 5 道关卡全过

- **检查什么**：身份认证 / 权限控制 / 输入校验 / 数据归属 / 注入防护
- **怎么检查**：读取 `docs/o-安全审查报告.md`，逐关核对
- **通过标准**：5 关每关都给"风险 / 处理位置（文件+行号）/ 处理规则 / 如何验证 / 是否通过"
- **证据要求**：安全审查报告路径 + 5 关结论

### 指标 5：水平越权测试通过

- **检查什么**：用户 A 不能访问用户 B 的资源
- **怎么检查**：执行水平越权测试用例（两个不同用户身份访问同一资源）
- **通过标准**：越权访问被拒绝（返回无权限/未授权），测试记录完整
- **证据要求**：越权测试命令 + 输出

### 指标 6：无字符串拼接用户输入

- **检查什么**：SQL / 命令 / 模板是否拼接用户输入
- **怎么检查**：搜索字符串拼接模式（f-string 拼 SQL、`os.system` 拼命令等）
- **通过标准**：全部使用参数化查询 / 安全 API；无拼接用户输入
- **证据要求**：搜索命令 + 结果（无命中或已整改）

### 指标 7：安全审查报告已落盘

- **检查什么**：`docs/o-安全审查报告.md` 是否存在
- **怎么检查**：读取文件，检查 5 关结论 + 整改清单（按严重/风险/建议分级）
- **通过标准**：报告落盘、整改清单全部处理、无 🔴 严重项残留
- **证据要求**：文件路径 + 整改清单处理状态

### 指标 8：已 Git 提交

- **检查什么**：Git 提交历史（每个模块一轮一提交）
- **怎么检查**：`git log --oneline`
- **通过标准**：至少与业务模块轮次数相当的提交数，含"通过业务门"之前的最后提交
- **证据要求**：`git log` 输出

---

## 自动验收流程

```
1. 智能体读取本文件的 8 项硬指标
2. 逐项检查（文件存在性 + 内容检查 + Git 历史）
3. 每项收集证据：
   - 文件路径
   - 关键内容行号
   - 命令输出（如 git log / 越权测试输出）
4. 生成验收报告：

   验收报告：业务门
   ================
   指标 1 P0 功能：✅ 通过
     证据：docs/a-立项文档.md 功能清单，P0 全部勾选完成
   指标 2 每轮验收：✅ 通过
     证据：docs/验收记录/模块A.md，第 3-20 行含命令输出
   ...
   指标 8 Git 提交：❌ 未通过
     原因：最后几个模块没提交
     整改建议：git commit -m "feat: 完成模块X并验收"

5. 全部通过 → 推进状态到上线门
6. 有未通过 → 列出整改清单，不推进状态
```

---

## 常见遗漏项（反模式检查）

智能体验收时，额外检查以下反模式：

- ⚠️ 一口气写完所有业务（必须拆轮，一轮一验收）
- ⚠️ 跳过每轮验收直接开下一个模块
- ⚠️ 改了规则不改真源文档
- ⚠️ 一个模块未验收就开下一个
- ⚠️ 安全只做"要不要登录"忽略水平越权
- ⚠️ 让 AI 自己写过滤规则代替参数化查询
- ⚠️ 整改时顺手改其他代码
- ⚠️ 安全审查不给证据，只说"已做安全处理"
- ⚠️ 用"基本完成""应该没问题"等模糊词
- ⚠️ 没有证据就标记"通过"
- ⚠️ 没过业务门就进入上线门

---

## 验收结果处理

### 全部通过

1. 更新 `PROJECT_STATUS.agent.md`：
   - `current_gate` 改为 `上线门`
   - `current_phase` 改为 `13`（上线前总验收）
   - `gate_entered_at` 改为今天
   - 验收清单全部勾选 ✅
2. Git 提交：`git commit -m "chore: 通过业务门"`
3. 提示用户：可以进入上线门了，下一步是上线前总验收

### 有未通过项

1. 生成整改清单（每项：未通过指标 + 原因 + 整改建议）
2. 不推进状态
3. 智能体协助整改，整改后重新执行本验收流程
4. 直到全部通过才推进状态
